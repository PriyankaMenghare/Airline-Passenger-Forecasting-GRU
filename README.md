# Airline-Passenger-Forecasting-GRU
A deep learning time series forecasting model using a GRU (Gated Recurrent Unit) network to predict monthly airline passenger numbers. Trained on the classic Box-Jenkins airline dataset with MinMax normalization and a 12-month lookback window. Built with TensorFlow/Keras.
